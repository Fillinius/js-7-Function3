const getDivisorCount = function getDivisorCount(number) {
  if (!number || typeof number != 'number') {
    return NaN
  }
  if (number <= 0 || number === 0) {
    alert(`${number} должно быть целым числом или больше нуля`)
  }
  else(number/number){
alert
  }
}

console.log(getDivisorCount(5))
